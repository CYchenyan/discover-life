import React from 'react';
import LoginOrRegister from '../../components/LoginOrRegister';

const Register = () => {
  const handleNext = () => {};
  return <LoginOrRegister type="register" onNext={handleNext} />;
};

export default Register;
